# Тесты бэкенда

Запуск (Node.js 22.5+):

```bash
node --test tests/*.test.js
```

- `auth.service.test.js` — регистрация, вход, расчёт нормы.
- `order.service.test.js` — расчёт суммы заказа, промокод, валидация.

Тесты используют временную БД в системной папке `tmp` и не трогают рабочие данные.
