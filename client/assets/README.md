# assets

Значки и изображения приложения.

- `icon-512.png` — иконка приложения (используется в `manifest.webmanifest`).

Фотографии товаров подгружаются по URL с сайта фабрики (см. `js/data/media-tags.js`).
